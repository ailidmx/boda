{
  "name": "boda-invitation",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "manifest": "node scripts/generate-media-manifest.mjs",
    "build": "npm run manifest && vite build",
    "preview": "vite preview",
    "links": "node scripts/generate-invitation-links.mjs",
    "favicons": "node scripts/generate-favicons.mjs",
    "test:profiles": "node --test tests/invitation-profile.test.mjs",
    "test:rules": "node --test tests/firestore.rules.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "favicons": "^7.3.1",
    "firebase-admin": "^12.7.0",
    "sharp": "^0.35.3",
    "vite": "^6.4.3"
  },
  "dependencies": {
    "cloudinary": "^2.10.0",
    "dotenv": "^17.4.2",
    "firebase": "^12.16.0"
  }
}
