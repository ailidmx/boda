/*
 * Public invitation media registry.
 *
 * Approved images should be placed in src/assets/approved, imported here, and
 * assigned to a slot. Vite will copy only imported assets into the build and
 * give them cache-safe fingerprinted filenames.
 *
 * Example:
 * import heroPhoto from "./assets/approved/hero-couple.webp";
 * export const MEDIA = { hero: heroPhoto };
 */
export const MEDIA = {
  hero: null,
};
